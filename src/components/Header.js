import React from "react";
import "../CSS/app.css";

export default () => {
  return (
    <div className="header">
      <i className="huge chevron left icon left-arrow" onMouseEnter={onMouseEvents}/>
      <i className="huge chevron right icon right-arrow" />
    </div>
  );
};

const onMouseEvents = (event)=>{


}
